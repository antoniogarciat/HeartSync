<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo de la barra de progreso -->
    <item android:id="@android:id/background">
        <shape android:shape="ring" android:innerRadiusRatio="2.5"
            android:thicknessRatio="16.0" android:useLevel="false">
            <solid android:color="#EC8E8E"/> <!-- Color del fondo -->
        </shape>
    </item>

    <!-- Barra de progreso principal -->
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="270">
            <shape android:shape="ring" android:innerRadiusRatio="2.5"
                android:thicknessRatio="16.0" android:useLevel="true">
                <solid android:color="#801B1F"/> <!-- Color de la barra de progreso -->
            </shape>
        </rotate>
    </item>
</layer-list>
